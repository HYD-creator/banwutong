services:
  banwutong:
    build: .
    restart: unless-stopped
    ports:
      - "4174:4174"
    environment:
      NODE_ENV: production
      PORT: 4174
      DATA_DIR: /app/data
      BACKUP_DIR: /app/data/backups
    volumes:
      - ./data:/app/data
